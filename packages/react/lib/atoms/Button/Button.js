import React from 'react';

const Button = ({ label }) => {
    return React.createElement("button", { className: "movlan-btn-container" }, label);
};

export { Button as default };
//# sourceMappingURL=Button.js.map
